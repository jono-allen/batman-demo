on:
  pull_request:
    types: [opened]

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - name: a_normal_comment
        uses: jono-allen/batman-team-mate@master
        with:
          percentage: 100
          actor: 'jono-allen'
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}